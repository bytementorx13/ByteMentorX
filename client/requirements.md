## Packages
framer-motion | Essential for the premium animations, scroll reveals, and hover effects requested.
react-scroll | Smooth scrolling for single-page navigation links.

## Notes
- Single-page application architecture
- Backend is expected to receive `POST /api/requests` with `serviceType`, `name`, `email`, `formData` (JSON), and `calculatedPrice`.
- No authentication is required.
